name: Dev CI

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:
    permissions:
      contents: write
    uses: ./.github/workflows/build-and-test.yml
    with:
      is_release: false
    secrets: inherit
